{
  "name": "@extensions/contract",
  "version": "1.0.0",
  "description": "Shared Zod schemas + inferred types for the NameSync API/WS contract, consumed by both the api and the frontend.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "zod": "^3.24.0"
  }
}
